---
- name: download rke
  get_url:
    url: "{{ rke_url }}"
    checksum: "{{ rke_checksum }}"
    dest: /usr/bin/rke
    mode: "+x"

- name: import cluster.yml config
  template:
    src: "cluster.yml.j2"
    dest: "/home/ansible/cluster.yml"
    mode: 0775
    force: yes
