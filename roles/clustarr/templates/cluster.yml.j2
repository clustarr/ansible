cluster_name: rancher

nodes:
  {% for host in groups['masters'] | default([]) -%}
  - address: {{ host }}
    user: ansible
    role:
      - controlplane
      - etcd
      - worker
  {%- endfor %}
  {% for host in groups['workers'] | default([]) -%}
  - address: {{ host }}
    user: ansible
    role:
      - worker
  {%- endfor %}

network:
  plugin: weave

ingress:
  provider: nginx
